$(function () {
    $(".dt_picker_compensation").datepicker({
        autoclose: true,
        todayHighlight: false,
        format: "mm/yyyy",
        startView: "year",
        minViewMode: "months",
    });
});